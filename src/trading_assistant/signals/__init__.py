"""Signal library: deterministic indicators, structure, events, regime, features.

The LLM analyst never computes an indicator — it receives a MarketFeatures object
(all math done here, in code) and interprets it.
"""
